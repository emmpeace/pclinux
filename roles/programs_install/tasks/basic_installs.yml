---
- name: Update all packages to their latest version
  apt:
    name: "*"
    state: latest
    update_cache: yes

- name: Install basics programs
  apt:
    name: "{{ item }}"
    state: latest
    update_cache: yes
  loop:
    - "keepassxc"
